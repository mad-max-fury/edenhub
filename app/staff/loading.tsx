import React from "react";

import { PageLoader } from "@/components/pageLoader/pageLoader";

const Loading = () => {
  return (
    <div>
      <PageLoader isOuterPage />
    </div>
  );
};

export default Loading;
