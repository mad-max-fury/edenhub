import AuthWrapper from "../components/authWrapper";
import { LoginForm } from "./components";

const Login = () => {
  return (
    <AuthWrapper>
      <LoginForm />
    </AuthWrapper>
  );
};

export default Login;
